import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class TodoItemTest {

    @Test
    void setName() {
        //Create an item and set values.
        //assert the result of get() and the input values.
    }

    @Test
    void setDate() {
        //Create an item and set values.
        //assert the result of get() and the input values.
    }

    @Test
    void setCompleteFlag() {
        //Create an item and set complete to 'true'
        //Call getIfComplete which should only work if the value is true.
    }

    @Test
    void get() {
        //Create an item and set values.
        //assert the result of get() and the input values.
    }

    @Test
    void getIfComplete() {
        //Create an item and set complete to 'true'
        //Call getIfComplete which should only work if the value is true.
    }
}